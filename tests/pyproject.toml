[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "poocam-tests"
version = "0.1.0"
dependencies = [
    "pytest>=8.4.2",
    "poocam-core",
]

requires-python = ">=3.13"
